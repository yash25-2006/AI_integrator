# Gemma 4 Integration Hub - Environment Configuration Example
# Do NOT commit real API keys to version control.

# Gemma 4 Provider Credentials (Optional default - can also be configured securely via UI)
GEMMA_API_KEY=
GEMMA_MODEL=gemma-4-26b-a4b-it
GEMMA_BASE_URL=https://generativelanguage.googleapis.com/v1beta/models

# Auxiliary Provider Credentials (Optional)
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.0-flash

# Server Port
PORT=3000
